---
name: Build RPM packages

on:
  # Triggers the workflow on push or pull request events but only for the master branch
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  build_packages:
    strategy:
      matrix:
        build_type:
          - auto
          - manual
        version:
          - 8
          - 7
    runs-on: ubuntu-20.04
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Setup python environment
        uses: actions/setup-python@v2
        with:
          python-version: "3.8"
          architecture: "x64"

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Build packages
        run: make ${{ matrix.build_type }}${{ matrix.version }}

  publish_packages:
    needs: build_packages
    if: github.ref == 'refs/heads/master'
    strategy:
      matrix:
        version:
          - 8
          - 7
    runs-on: ubuntu-20.04
    steps:
      # following tasks will require adjustment:
      - if: env.RPM_GPG_KEY_SECRET != ''
        run: echo openssl aes-256-cbc -k ${RPM_GPG_KEY_SECRET} -in secret.asc.enc -out secret.asc -d

      - if: env.RPM_GPG_KEY_PASSPHRASE != ''
        run: echo ${RPM_GPG_KEY_PASSPHRASE} > .passphrase

      - if: env.TRAVIS_REPO_SLUG == 'lest/prometheus-rpm'
        run: echo make publish${{ matrix.version }}
