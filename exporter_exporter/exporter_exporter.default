EXPORTER_EXPORTER_OPTS='-config.file /etc/prometheus/expexp.yaml -config.dirs /etc/prometheus/expexp.d'

# You can use exporter_exporter with TLS to encrypt the traffic, and at the same
# time enforce strong mutual authentication between the nodes and the prometheus
# server. e.g.,
#EXPORTER_EXPORTER_OPTS='-web.listen-address= -web.tls.listen-address=:9998 \
# -web.tls.cert=/etc/prometheus/ssl/prom_node_cert.pem \
# -web.tls.key=/etc/prometheus/ssl/prom_node_key.pem \
# -web.tls.ca=/etc/prometheus/ssl/prometheus_cert.pem \
# -web.tls.verify' 